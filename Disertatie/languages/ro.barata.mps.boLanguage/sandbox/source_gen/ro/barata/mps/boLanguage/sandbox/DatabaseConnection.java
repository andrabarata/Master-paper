package ro.barata.mps.boLanguage.sandbox;

/*Generated by MPS */

import java.sql.Connection;
import java.sql.SQLException;
import java.sql.DriverManager;

public class DatabaseConnection {
  public static Connection getConnection() throws SQLException, ClassNotFoundException {

    try {
      Class.forName("oracle.jdbc.driver.OracleDriver");
    } catch (ClassNotFoundException e) {
      e.printStackTrace();
    }

    String driverName = "jdbc:oracle:thin";
    String ip = "localhost";
    int port = 1521;
    String sid = "xe";
    String username = "disertatie";
    String password = "disertatie";
    String connectionValue = driverName + ":@" + ip + ":" + port + ":" + sid;
    System.out.println(connectionValue);
    Connection con = DriverManager.getConnection(connectionValue, username, password);
    return con;
  }

}
