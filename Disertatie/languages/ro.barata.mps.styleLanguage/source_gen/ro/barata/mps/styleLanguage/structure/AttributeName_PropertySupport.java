package ro.barata.mps.styleLanguage.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class AttributeName_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<AttributeName> constants = ListSequence.fromList(AttributeName.getConstants()).iterator();
    while (constants.hasNext()) {
      AttributeName constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<AttributeName> constants = ListSequence.fromList(AttributeName.getConstants()).iterator();
    while (constants.hasNext()) {
      AttributeName constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    AttributeName constant = AttributeName.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
